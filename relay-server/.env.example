# Relay server configuration
RELAY_API_KEY=change-me-to-a-strong-secret

# Optional: comma-separated proxy URLs for IP rotation
# PROXY_URL=http://user:pass@proxy1:3128,http://user:pass@proxy2:3128

# Probe timing jitter (ms)
RELAY_MIN_DELAY_MS=200
RELAY_MAX_DELAY_MS=1500

# Server port
PORT=4001
